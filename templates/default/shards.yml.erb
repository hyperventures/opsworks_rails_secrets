# This file is generated by the opsworks_rails_secrets cookbook. Update it by setting the
# stack's custom JSON and executing the opsworks_rails_secrets::configure recipe.

<% mash = JSON.parse(@env.dup.to_json) %>

octopus:
  environments:
    - production
  production:
    shards:
      <%= mash.to_yaml %>
